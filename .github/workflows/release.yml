name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    uses: Travbz/undercurrent-workflows/.github/workflows/main-release.yaml@main
    with:
      floating_major_tag: true
